#include "holberton.h"

void reverse_array(int *a, int n)
{
  int i = 0;
  int tin = 0;

  n = n - 1;

  while (i <= n + 1; i >= 0; i--)
    {
      tin = 0;
      tan = a[j];
      a[i] = tan;
      a[j] = tin;
      j++;
      i--;
    }
} 
