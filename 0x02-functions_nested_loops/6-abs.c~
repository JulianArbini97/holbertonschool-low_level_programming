#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
int _abs(int)
{
  int n;

  if (n < 0)
    {
      _putchar (-n);
    }
  else 
    { 
      _putchar (n);
    }
}
