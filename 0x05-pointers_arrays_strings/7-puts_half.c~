#include "holberton.h"
#include <string.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void puts_half(char *str)
{
  int i = 0;
  int j = strlen(str);

  while (i >= j/2)
    {
      _putchar (str[i]);
    }
  _putchar (10);
}
 
