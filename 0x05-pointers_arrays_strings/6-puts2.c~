#include "holberton.h"
#include <string.h>
/**
 * main - check the code for Holberton School students.
 *
 * Return: Always 0.
 */
void puts2(char *str)
{
  int i = 0;
  for (i = 0; i < strlen(str); i+=2)
    {
      _putchar(str[i]);
    }
  _putchar (10);
}
